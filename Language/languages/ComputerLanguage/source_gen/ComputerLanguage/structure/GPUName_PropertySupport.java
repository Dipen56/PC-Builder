package ComputerLanguage.structure;

/*Generated by MPS */

import jetbrains.mps.smodel.PropertySupport;
import java.util.Iterator;
import jetbrains.mps.internal.collections.runtime.ListSequence;

public class GPUName_PropertySupport extends PropertySupport {
  public boolean canSetValue(String value) {
    if (value == null) {
      return true;
    }
    Iterator<GPUName> constants = ListSequence.fromList(GPUName.getConstants()).iterator();
    while (constants.hasNext()) {
      GPUName constant = constants.next();
      if (value.equals(constant.getName())) {
        return true;
      }
    }
    return false;
  }
  public String toInternalValue(String value) {
    if (value == null) {
      return null;
    }
    Iterator<GPUName> constants = ListSequence.fromList(GPUName.getConstants()).iterator();
    while (constants.hasNext()) {
      GPUName constant = constants.next();
      if (value.equals(constant.getName())) {
        return constant.getValueAsString();
      }
    }
    return null;
  }
  public String fromInternalValue(String value) {
    GPUName constant = GPUName.parseValue(value);
    if (constant != null) {
      return constant.getName();
    }
    return "";
  }
}
